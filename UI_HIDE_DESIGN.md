# 플레이 화면 UI 숨김·표시 설계

## 목표

플레이 중 대사와 조작 UI를 잠시 숨겨 배경·캐릭터·CG를 감상할 수 있게 한다. UI를
숨긴 상태에서 대사가 넘어가거나 선택지가 사라져 진행을 놓치는 일은 없어야 한다.

## 진입과 복귀

- 플레이 화면 우측 상단에 `UI 숨기기` 버튼(눈 아이콘)을 둔다.
- UI가 숨겨진 동안 모바일의 화면 탭과 데스크톱의 화면 클릭은 UI 복구만 수행한다.
  복구를 위한 첫 입력으로 대사를 진행하지 않는다.
- 키보드 `H`는 숨김·표시 전환, `Esc`는 숨겨진 UI 복구에 사용한다.

## 숨김 범위

숨김 대상은 챕터 태그, 저장·설정 버튼, 대사창, 상황 설명이다. 배경·캐릭터·CG는
계속 표시한다. 선택지나 이름 입력이 활성화된 동안에는 숨김을 허용하지 않아 필수
입력 UI가 사라져 게임이 멈추는 상황을 방지한다.

의상 선택, 후원 확인, 화면 전환, CG·엔딩 크레딧, 설정·저장 화면에서는 UI를 항상
표시 상태로 유지한다.

## 진행 정지

UI를 숨긴 동안에는 타자기 출력·자동 진행·화면 클릭 진행을 일시정지한다. 배경음악은
계속 재생한다. 기존 `onyuGamePaused` 구조를 확장해 설정 화면 일시정지와 UI 숨김
일시정지를 사유별로 관리한다.

## 상태와 저장

숨김 상태는 현재 플레이 세션에서만 유지하며 세이브·localStorage에는 저장하지 않는다.
새 게임·이어하기·챕터 진입·플레이 화면 복귀 시에는 UI를 표시 상태로 시작한다.

## 구현 메모

- `#screen-play.is-ui-hidden` 클래스로 시각 상태를 제어한다.
- 숨겨진 요소는 `opacity: 0`과 `pointer-events: none`을 함께 사용한다.
- 숨김·표시 이벤트를 `play_ui_hidden`/`play_ui_shown`으로 집계한다.
- 데스크톱 마우스, 모바일 pointer/touch, 전체화면, 전환 잠금, 모션 줄이기 설정을
  함께 검증한다.
